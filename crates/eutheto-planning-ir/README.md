<!-- SPDX-License-Identifier: Apache-2.0 -->

# `eutheto-planning-ir` (reserved)

This directory reserves the roadmap boundary for **future solver-neutral planning representation, capability metadata, provenance, and projection contracts.** No `Cargo.toml` is present, so this is not a Cargo workspace member, implemented crate, or published API.

## Boundary

It must not depend on official domain packs, Tauri, persistence, or OR-Tools/Pumpkin objects.

The owning roadmap phase must confirm that this boundary still warrants a separate crate, choose its dependencies, and add implementation and tests. The exact future crate inventory is an explicit architecture gate; the reserved name alone does not settle it.
