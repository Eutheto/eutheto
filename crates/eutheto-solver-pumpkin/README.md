<!-- SPDX-License-Identifier: Apache-2.0 -->

# `eutheto-solver-pumpkin` (reserved)

This directory reserves the roadmap boundary for **future experimental in-process Pumpkin adapter.** No `Cargo.toml` is present, so this is not a Cargo workspace member, implemented crate, or published API.

## Boundary

It remains deferred to Phase 08 and must satisfy ownership, dedicated-thread, cancellation, panic, compatibility, packaging, and benchmark gates before implementation.

The owning roadmap phase must confirm that this boundary still warrants a separate crate, choose its dependencies, and add implementation and tests. The exact future crate inventory is an explicit architecture gate; the reserved name alone does not settle it.
