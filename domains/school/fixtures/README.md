<!-- SPDX-License-Identifier: Apache-2.0 -->

# School `fixtures` boundary

This directory reserves future reviewed examples and conformance inputs for the official **school timetable and block planning** pack. It contains no implementation, manifest, public format, or compatibility promise in Phase 00.

## Boundary

Fixtures must be synthetic or sanitized, deterministic, bounded, and versioned when their format becomes public. This directory does not define a scenario schema and must not contain captured user data.

The owning roadmap phase must approve the concrete crate/package inventory and add real behavior with its contracts and tests. Keeping this path does not make it a Cargo or pnpm workspace member.
